CONFIGURATION PlantConfig
RESOURCE Res1 ON PLC
    TASK FastTask(INTERVAL := T#10ms, PRIORITY := 1);
    TASK SlowTask(INTERVAL := T#100ms, PRIORITY := 10);

    PROGRAM P1 WITH FastTask : Main;
    PROGRAM P2 WITH SlowTask : Main;
END_RESOURCE
END_CONFIGURATION
